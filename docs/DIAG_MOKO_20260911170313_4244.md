# Diagnostic — `MOKO_20260911170313_4244`

Date diagnostic : 2026-09-12 (API locale + log Flutter `dev-knb`).

## Faits

| Source | Valeur |
|--------|--------|
| Référence | `MOKO_20260911170313_4244` |
| Gateway | `PD202609118XCN3QBE2H16B` |
| Flutter check | `status: pending`, `statusDescription: Error`, `idPaiement: null`, `amount: 209 CDF` |
| SMS opérateur | Débit 209 Fc, FreshPay facture `8273001`, 11-09-2026 |
| DB locale `dev-knb_db` | **0** ligne `TransactionsMoko` (la tx vit sur le serveur distant) |

## Interprétation

1. SMS ≠ événement API : pas de création `Paiements` sans `ConfirmerPayIn`.
2. Check a renvoyé `Status: Error` → `IsDefinitiveSuccess = false` → pas de confirm.
3. Soft Error + fenêtre USSD (ou soft hors fenêtre avant correctif) → `pending` + `isDefinitive: false`.
4. Callback : `CallbackUrl` = `https://dev-knb.asdc-rdc.org/api/MokoAfrika/callback` + `HmacKey` → webhook sans `X-Signature` = **401**.

## SQL ops (serveur distant)

```sql
SELECT Reference, Status, IdPaiement, StatusDescription,
       LENGTH(IFNULL(RawCallback,'')) AS LenCb,
       LENGTH(IFNULL(RawResponse,'')) AS LenRr,
       LEFT(IFNULL(RawCallback,''), 2000) AS RawCallback,
       LEFT(IFNULL(RawResponse,''), 2000) AS RawResponse
FROM TransactionsMoko
WHERE Reference = 'MOKO_20260911170313_4244';
```
